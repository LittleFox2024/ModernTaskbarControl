# ModernTaskbarControl

This is a remake of the old taskbar settings dialog before Windows 10 replaced it with the Settings app.
It is written in C# and XAML to make a WinUI 3 fluent style.

It uses simple registry checks to check for and apply settings.
